# v1.4.3

## 优化功能

- 优化项目管理表格页批量维护能力：列表支持多选项目，并在选中后提供“修改投放状态”入口，调用批量更新接口统一修改 `adStatus`，单次限制最多 500 个项目（src/pages/project-table/index.tsx, src/services/project/api.ts, src/services/project/types.ts）。
- 项目报表增加 `appPlatform` 筛选，并在项目编码列展示应用平台 tag，便于按 `iOS / Android` 维度快速识别与筛选项目（src/pages/report/project/index.tsx, src/pages/project/constants.ts, src/services/report/typings.d.ts）。
- 优化项目管理表格页字段维护能力：新增 `appPlatform`（应用平台）字段展示与编辑，并支持列表多选后批量更新应用平台，保持与现有项目接口和详情资源绑定联动刷新（src/pages/project-table/index.tsx, src/pages/project-table/fields.ts, src/services/project/api.ts, src/services/project/types.ts）。
- 优化项目管理表格页应用信息分组：`appPlatform` 调整到“应用信息”中展示，并在新建、编辑、批量修改时统一使用 `IOS` / `Android` 下拉选择输入（src/pages/project-table/fields.ts, src/pages/project-table/index.tsx, src/pages/project/constants.ts）。
- 优化项目管理表格页批量维护能力：新增“批量修改部门”，支持从现有部门列表下拉选择或手动输入新部门，并可通过清空输入框提交 `null` 清空部门（src/pages/project-table/index.tsx, src/services/project/api.ts, src/services/project/types.ts, docs/api/project_api.md）。
- 新增项目管理 CSV 导入能力：支持上传带表头 CSV、勾选导入字段并完成字段映射、预览前 20 行映射结果后通过 `batch-save` 按 `projectCode` 执行新增或更新（src/pages/project-table/index.tsx, src/pages/project-table/components/ProjectBatchImportModal.tsx, src/services/project/api.ts, src/services/project/types.ts, docs/api/project_api.md）。
- 优化项目报表广告收入展示：广告收入列改为支持展示 `广告收入（广告收益差值）`，并在统计字段选择区与列表表头补充 `adRevenueNow / adRevenueDiff` 字段释义，降低新增口径理解成本（src/pages/report/project/index.tsx, src/components/report/UniversalReportTable.tsx）。
- 优化项目趋势 Dashboard 收益分析：新增“广告收益对比趋势”图，使用 `adRevenue`、`adRevenueNow`、`adRevenueDiff` 三线对比项目广告收益变化（src/pages/report/project-trend/index.tsx）。
- 优化项目趋势 Dashboard 查询口径：`groupBy` 统一默认补充 `projectCode`，避免趋势页各类查询遗漏项目维度分组（src/pages/report/project-trend/index.tsx）。
- 优化项目趋势 Dashboard 国家排行交互：国家维度数据改为首次查询后前端本地切换 `广告收入 / 利润 / 新增用户`，并将占比小于 `0.1%` 的国家合并为 `其他`，减少重复请求与噪音项（src/pages/report/project-trend/index.tsx）。
- 优化项目趋势 Dashboard 广告收入卡片展示：将辅助信息收敛为 `最新收入（差值）` 单行弱化文本，并通过 tooltip 说明字段含义，同时兼容 `summary` 缺失时回退最新日报行（src/pages/report/project-trend/index.tsx, docs/api/project_report_query_api.md）。
- 优化项目趋势 Dashboard 广告收入卡片展示：在主值右上角增加 `AdRevenueNow / AdRevenue` 比例角标，保留 1 位小数，便于快速观察最新收入占比（src/pages/report/project-trend/index.tsx）。

## Bug 修复

- 修复项目趋势 Dashboard 切换国家排行指标时触发整页刷新和重复请求的问题，并将广告收益对比趋势改为使用 `adRevenueNow + |adRevenueDiff|` 的堆叠面积图展示，避免负差值导致柱体分离（src/pages/report/project-trend/index.tsx）。
- 修复项目管理 CSV 导入中“上传后直接跳预览导致字段映射步骤感知缺失”的问题，导入弹窗改为显式的上传、字段映射、预览、结果四步流程（src/pages/project-table/components/ProjectBatchImportModal.tsx）。
- 修复项目管理 CSV 导入对缺少 `projectCode` 行整体拦截的问题，此类行现在在预览中标记为“跳过”并自动排除，不再阻塞其余有效数据导入（src/pages/project-table/components/ProjectBatchImportModal.tsx）。
- 修复项目管理 CSV 导入对标准多行字段解析错误的问题，改为使用 `papaparse` 正确处理引号包裹字段中的换行、逗号和双引号转义，避免 `Admob广告ID` 等多行单元格错位到 `projectCode`（src/pages/project-table/components/ProjectBatchImportModal.tsx, package.json, package-lock.json）。

## 优化功能补充

- 扩展项目管理 CSV 导入字段范围为项目可维护字段全集，复用 `PROJECT_TABLE_FIELDS` 生成目标字段列表，并补充 `status` 字段，避免只维护少数字段的分叉配置（src/pages/project-table/components/ProjectBatchImportModal.tsx, src/pages/project-table/fields.ts, src/services/project/types.ts）。
