## 优化功能

- 优化用户管理中的封禁 IP 列表展示：将 IP、类型/来源、关联用户、封禁原因/创建时间收拢为紧凑信息列，减少横向滚动和重复列占用，并保留类型更新与删除操作入口（src/pages/user-manage/components/BlockedIpModal.tsx）。
- 优化封禁 IP 列表关联用户展示：移除用户与管理员下方的 ID 行，仅保留邮箱信息，降低行内信息噪音（src/pages/user-manage/components/BlockedIpModal.tsx）。
- 优化工单管理列表展示：新增 `latest_message` 最新记录列，超长内容单行省略并保留悬浮查看，避免消息内容换行撑高表格（`src/pages/ticket/index.tsx`, `src/services/ticket/typings.d.ts`）
- 优化工单管理列表交互：最新记录列支持点击直达详情，用户邮箱列改为单行省略并保留悬浮查看，减少长文本挤压表格（`src/pages/ticket/index.tsx`）
- 优化工单管理列表字段展示：主题列增加最小宽度，新增用户注册元数据中的应用 ID 列，并兼容 `register_metadata/register_matedata.app_id` 两种返回命名（`src/pages/ticket/index.tsx`, `src/services/ticket/typings.d.ts`）
- 优化工单管理列表可读性：主题列最小宽度调整为 250，最新记录列改为蓝色可点击样式并增加“点击查看详情”提示，强化可交互感知（`src/pages/ticket/index.tsx`）
- 优化项目管理导入预览表字段展示：长文本字段统一改为单行省略，并在悬浮时展示保留换行的完整内容，避免预览行高被多行字段撑开（`src/pages/project-table/components/ProjectBatchImportModal.tsx`）

## Bug 修复

- 修复广告变现同步节点弹窗点击下一页不刷新数据的问题：同步节点列表改为受控分页，并在翻页时携带 `page/pageSize` 重新请求接口（`src/pages/ad/ad-revenue/components/SyncServersModal.tsx`, `src/services/ad/api.ts`）
- 修复版本检查接口首屏重复请求：为 `guest/version/latest` 增加进行中请求共享与 3 秒短缓存，避免浏览器刷新时页头、版本弹窗和运行时检查各自重复拉取（`src/services/version/api.ts`）
## 新增功能

- 新增代理流量账号管理弹窗中的“流量分配”操作：支持按账号填写目标用户 ID、目标用户名和分配流量后直接调用分配接口，成功后刷新账号列表与看板统计（`src/pages/traffic-platform/dashboard/components/AccountManageModal.tsx`, `src/services/traffic-platform/api.ts`）
- 新增自动化策略 `traffic_platform` 模块的 `traffic_allocation` 流量分配动作：支持配置目标用户 ID、目标用户名和分配流量 GB，并在提交时映射为后端字段 `target_user_id/target_username/amount_gb`（`src/components/AutomationRulesEntry.tsx`, `src/services/automation-rules/typings.d.ts`）
